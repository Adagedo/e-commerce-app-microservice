spring:
  datasource:
    url: jdbc:mysql://localhost:3306/customerdb?useSSL=false&serverTimezone=UTC
    username: root
    password: adagedo@#$%1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform:
      dialect: org.hibernate.dialect.MYSQL8Dialect


server:
  port: 5000